[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
# Sprawdzanie błędów (E), nieużywanych zmiennych (F) i sortowanie importów (I)
select = ["E", "F", "I"]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
